<?xml version='1.0' encoding='UTF-8'?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>net.jangaroo.extas.examples</groupId>
  <artifactId>ext-as-example-parent</artifactId>
  <version>0.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>ext-as-example-parent</name>
  <description>
    A Maven parent POM for all Ext AS examples.
  </description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <jangaroo-plugin.version>3-preview-1-SNAPSHOT</jangaroo-plugin.version>
    <jangaroo-libs.version>3-preview-1-SNAPSHOT</jangaroo-libs.version>
    <jetty.plugin.version>8.1.3.v20120416</jetty.plugin.version>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>net.jangaroo</groupId>
          <artifactId>jangaroo-maven-plugin</artifactId>
          <version>${jangaroo-plugin.version}</version>
          <extensions>true</extensions>
        </plugin>

        <plugin>
          <groupId>org.mortbay.jetty</groupId>
          <artifactId>jetty-maven-plugin</artifactId>
          <version>${jetty.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <directory>src/main/web-resources</directory>
        <targetPath>META-INF/resources</targetPath>
      </resource>
    </resources>
  </build>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>net.jangaroo</groupId>
        <artifactId>jangaroo-runtime</artifactId>
        <version>${jangaroo-plugin.version}</version>
      </dependency>

      <dependency>
        <groupId>net.jangaroo</groupId>
        <artifactId>jangaroo-browser</artifactId>
        <version>${jangaroo-libs.version}</version>
      </dependency>

      <dependency>
        <groupId>net.jangaroo</groupId>
        <artifactId>joounit</artifactId>
        <version>${jangaroo-libs.version}</version>
      </dependency>

      <dependency>
        <groupId>net.jangaroo</groupId>
        <artifactId>ext-as</artifactId>
        <version>4.2-1-SNAPSHOT</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

</project>
